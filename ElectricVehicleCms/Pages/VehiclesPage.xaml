
<Page x:Class="ElectricVehicleCms.Pages.VehiclesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ElectricVehicleCms.Pages"
      mc:Ignorable="d"
      d:DesignWidth="1100"
      d:DesignHeight="700"
      Background="{StaticResource SurfaceBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryBrush}"
                           Text="Electric vehicles" />
                <TextBlock x:Name="CurrentRoleTextBlock"
                           Foreground="#556572"
                           Text="Logged in as:" />
            </StackPanel>

            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button x:Name="AddVehicleButton"
                        Width="120"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="AddVehicleButton_Click"
                        ToolTip="Add new electric vehicle"
                        Content="Add" />
                <Button x:Name="DeleteSelectedButton"
                        Width="140"
                        Style="{StaticResource DangerButtonStyle}"
                        Click="DeleteSelectedButton_Click"
                        ToolTip="Delete selected rows"
                        Content="Delete selected" />
                <Button Width="120"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="LogoutButton_Click"
                        ToolTip="Return to login page"
                        Content="Logout" />
            </StackPanel>
        </DockPanel>

        <CheckBox x:Name="SelectAllCheckBox"
                  Grid.Row="1"
                  Margin="0,0,0,10"
                  VerticalAlignment="Center"
                  Checked="SelectAllCheckBox_Checked"
                  Unchecked="SelectAllCheckBox_Unchecked"
                  Content="Select all rows"
                  Cursor="Hand"
                  ToolTip="Select or unselect every row" />

        <DataGrid x:Name="VehiclesDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="72"
                  ItemsSource="{Binding Vehicles}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn x:Name="SelectColumn" Header="Select"
                                        Width="85"
                                        Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTemplateColumn Header="Vehicle" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center">
                                <Hyperlink NavigateUri="{Binding Name}"
                                           RequestNavigate="VehicleHyperlink_RequestNavigate"
                                           ToolTip="Open details">
                                    <Run Text="{Binding Name}" />
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Image" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="4"
                                    BorderBrush="#D4DCE2"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <Image Width="110"
                                       Height="58"
                                       Stretch="UniformToFill"
                                       Source="{Binding FullImagePath}" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Date added"
                                    Width="165"
                                    Binding="{Binding DateAdded, StringFormat=yyyy-MM-dd HH:mm}" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Margin="0,10,0,0"
                   Foreground="#5C6B76"
                   Text="Admin opens edit page by clicking the vehicle name. Visitor opens details page." />
    </Grid>
</Page>
